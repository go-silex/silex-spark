# silex-spark

Miroir public read-only. SoT = `go-silex/spark` `plugins/silex-spark/`.
B.3 : racine = plugin — Bun n'installe pas un git-subdir ; spark privé ; ¬subtree (historique tokenisé) → rsync-squash.
Contribuer dans spark, pas ici (`CONTRIBUTING.md`).
